const print =()=>{
    let num1 = document.getElementById('num1').value;
    let num2 = document.getElementById('num2').value;
    document.getElementById('result').innerHTML = `Initialise the number with the value of ${num1},Assign the same variable number with of ${num2}`;
}